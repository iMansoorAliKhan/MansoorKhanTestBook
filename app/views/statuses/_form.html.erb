<%= simple_form_for @status, html: { class: 'form-horizontal' }  do |f| %>
<% if @status.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>
  <ul>
    <% @status.errors.full_messages.each do |message| %>
    <li class="label label-danger"><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
<div class="container">
  <div class="row">
    <div class="field col-md-6">
    <%= f.input :user_id ,collection: User.all.collect{|u|[u.id]}, :label => "user name:"  %>
      <%= f.input :content ,input_html: { class: 'form-control' }%>
      <div class="actions">
        <%= f.button :submit ,:class=>"pull-left clear-affix"%>
      </div><br/>
    </div>
  </div>
</div>
<% end %>